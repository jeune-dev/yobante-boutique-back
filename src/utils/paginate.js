// ─────────────────────────────────────────────────────────────
// utils/paginate.js — Helper de pagination
// ─────────────────────────────────────────────────────────────

// TODO: paginate(query)
//   - query : { page=1, limit=10 }
//   - Calculer offset = (page - 1) * limit
//   - Retourner { limit, offset }

// TODO: paginateResult(count, page, limit)
//   - Retourner { totalItems: count, totalPages: Math.ceil(count/limit), currentPage: page }
