// ─────────────────────────────────────────────────────────────
// server.js — Point d'entrée du serveur
// ─────────────────────────────────────────────────────────────

// TODO: importer dotenv et appeler dotenv.config()
// TODO: importer app depuis ./app
// TODO: importer sequelize depuis ./config/db
// TODO: importer le logger depuis ./utils/logger

// TODO: définir PORT depuis process.env.PORT ou 5000

// TODO: synchroniser la base de données avec sequelize.authenticate()
//       puis lancer app.listen(PORT, ...)
//       logger.info(`Serveur démarré sur le port ${PORT}`)

// TODO: gérer les erreurs non capturées
//   process.on('unhandledRejection', ...)
//   process.on('uncaughtException', ...)
