// ─────────────────────────────────────────────────────────────
// routes/admin/user.route.js   — Préfixe : /api/admin/users
// ─────────────────────────────────────────────────────────────
// const router = require('express').Router()
// const ctrl = require('../../controllers/admin/user.controller')
// const { auth } = require('../../middlewares/auth.middleware')
// const { admin } = require('../../middlewares/admin.middleware')

// GET    /api/admin/users              -> ctrl.getAll      [auth, admin]
// GET    /api/admin/users/export       -> ctrl.exportUsers [auth, admin]
// GET    /api/admin/users/:id          -> ctrl.getOne      [auth, admin]
// PATCH  /api/admin/users/:id/bloquer  -> ctrl.bloquer     [auth, admin]
// PATCH  /api/admin/users/:id/activer  -> ctrl.activer     [auth, admin]
// DELETE /api/admin/users/:id          -> ctrl.remove      [auth, admin]

// module.exports = router
