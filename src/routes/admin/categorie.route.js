// ─────────────────────────────────────────────────────────────
// routes/admin/categorie.route.js   — Préfixe : /api/admin/categories
// ─────────────────────────────────────────────────────────────
// const router = require('express').Router()
// const ctrl = require('../../controllers/admin/categorie.controller')
// const { auth } = require('../../middlewares/auth.middleware')
// const { admin } = require('../../middlewares/admin.middleware')

// GET    /api/admin/categories      -> ctrl.getAll  [auth, admin]
// POST   /api/admin/categories      -> ctrl.create  [auth, admin]
// GET    /api/admin/categories/:id  -> ctrl.getOne  [auth, admin]
// PUT    /api/admin/categories/:id  -> ctrl.update  [auth, admin]
// DELETE /api/admin/categories/:id  -> ctrl.remove  [auth, admin]

// module.exports = router
